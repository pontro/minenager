services:
  dashboard:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
      - "25565:25565"
    volumes:
      - ./data:/data
      - ./app:/code/app
    environment:
      - PYTHONUNBUFFERED=1
